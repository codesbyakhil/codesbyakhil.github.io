source "https://rubygems.org"
# Mirrors the exact environment GitHub Pages uses to build the site.
# Needed ONLY for local preview (bundle exec jekyll serve) — deploying
# to GitHub Pages requires no local Ruby at all.
gem "github-pages", group: :jekyll_plugins
